type User {
  id: Int!
  username: String!

}

type Post {
  id: Int!
  title: String!
  writer: User!
}

type Query {
  id: Int!
  me: User!
  user(userId: Int!): User!
  post(postId: Int!): Post!
}
